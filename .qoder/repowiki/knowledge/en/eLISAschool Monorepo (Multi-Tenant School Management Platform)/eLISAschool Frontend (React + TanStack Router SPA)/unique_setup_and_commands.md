Route tree is auto-generated by `@tanstack/router-cli`; run `pnpm dev` (Vite on port 7001, host 0.0.0.0) or `pnpm build` (runs `tsc -b` then `vite build`). Favicons are regenerated via `pnpm generate-favicons` which executes `scripts/generate-favicons.ts` using Sharp.