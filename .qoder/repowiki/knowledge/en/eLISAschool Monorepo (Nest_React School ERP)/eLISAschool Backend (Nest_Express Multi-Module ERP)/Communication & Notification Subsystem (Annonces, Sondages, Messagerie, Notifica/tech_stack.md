TypeORM entities with soft-delete (`DeleteDateColumn`); Server-Sent Events backed by Redis pub/sub (`redisService.getSubscriberClient()` + `publish('messagerie:events', ...)`) for multi-instance fan-out; optional Socket.IO integration injected lazily into `SondageWebSocketService`; `node-cron` for scheduled tasks (cantine reminders, poll lifecycle, notification queue drain); provider registry pattern for extensible notification channels (SMTP/Firebase/Twilio/etc.).