React 19 + TypeScript, Vite build, TanStack Router (file-based routes under `src/routes/`), TanStack Query v5 (`@tanstack/react-query`) with a centralized `QueryClient`, Zustand + `persist` middleware for client state, i18next + `react-i18next` with statically bundled `fr`/`en` JSON namespaces, Sonner for toasts, Tailwind CSS via `globals.css`, Lucide icons, and a custom fetch-based `ApiClient` class handling JWT access/refresh rotation and multi-tenant `etablissementId` enforcement.