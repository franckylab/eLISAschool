TypeORM with PostgreSQL via DataSource pattern (entities/migrations loaded dynamically based on NODE_ENV); Zod for runtime environment variable validation with schema inference; Swagger UI (swagger-ui-express) for interactive API documentation at `/api/docs`; JWT authentication with RS256/HMAC signing referenced in env.config.ts (JWT_SECRET, JWT_EXPIRES_IN); Redis configured for caching/session storage (REDIS_HOST, REDIS_PORT in env.config).