Express 4 + TypeScript 5, TypeORM 0.3 with PostgreSQL driver (`pg`), JWT (`jsonwebtoken`) for sessionless auth, bcryptjs for hashing, Zod for runtime env/DTO validation, Winston for logging, Helmet/CORS/express-rate-limit for security, ioredis for optional caching, Firebase Admin for push notifications, Twilio for SMS, Nodemailer for email, Sharp for image processing, Swagger UI for API docs, Jest for tests.