import { FondAnime } from './fond-anime';

/**
 * Fond principal du layout authentifié.
 * Dégradé thématique + quadrillage cahier (voir fond-anime.tsx).
 */
export function FondPrincipalBackground() {
    return <FondAnime />;
}
